:css
  .filter {
    display: none;
  }

:javascript
  $(document).ready(
    function(){
      setInterval(function(){
        $("#incedents").load('#{request.fullpath} #incedents');
      }, 180000);
    });

#incedents
  = render 'incedents'

= link_to glyph(:plus)+' Новая жалоба', new_incedent_path, class: 'btn btn-mini', data: { keybinding: 'ins' } if can? :create, Incedent
%br
%br
